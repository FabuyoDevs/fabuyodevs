# ==========================================================================
# FABUYO — .gitignore
# Static site (HTML/CSS/vanilla ES modules) + Firebase Hosting
# --------------------------------------------------------------------------
# Firebase configuration is generated during the Vercel build and must never
# be committed. Service-account / admin-SDK files also grant full access.
# ==========================================================================


# ----- Secrets & credentials (NEVER commit) -------------------------------
js/firebase-config.js
.env
.env.*

# Firebase Admin SDK service accounts — full backend access, bypasses rules
*serviceAccount*.json
*service-account*.json
*-firebase-adminsdk-*.json
firebase-adminsdk*.json
*secret*.json
*credentials*.json
*.key.json
*.pem
*.key
*.p12
*.p8
*.keystore
*.jks

# ----- Firebase tooling ---------------------------------------------------
.firebase/
firebase-debug.log*
firebase-debug.*.log*
firestore-debug.log*
database-debug.log*
pubsub-debug.log*
ui-debug.log*

# Local Emulator Suite state / exported data
.runtimeconfig.json
firebase-export-*/
emulator-data/

# NOTE: keep these committed — they define your deploy target:
#   firebase.json, .firebaserc, firestore.rules, storage.rules, firestore.indexes.json


# ----- Node / tooling -----------------------------------------------------
# (only present if you run `npx serve`, linters, or add tooling later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/
.pnpm-store/
.eslintcache
.cache/
.parcel-cache/
.temp/
tmp/


# ----- Build / deploy output ----------------------------------------------
dist/
build/
out/
.vercel/
.netlify/


# ----- Editors & IDEs -----------------------------------------------------
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*.sublime-workspace
*.sublime-project
.history/


# ----- Operating system junk ----------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride
Icon
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk


# ----- Logs & local scratch files -----------------------------------------
*.log
logs/
*.local
scratch/
TODO.local.md


# ----- Media working files (keep the repo light) --------------------------
# Source screenshots/mockups you compress before uploading to the CMS
*.psd
*.ai
*.sketch
*.fig
*.xcf
/assets/raw/
/screenshots/raw/
